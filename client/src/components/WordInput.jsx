import React,{useState} from 'react'; export default function WordInput({onSubmit,lastLetter,currentPlayer}){const [word,setWord]=useState(''); function handle(e){e.preventDefault(); onSubmit(word); setWord('');} return (<form onSubmit={handle}><h3>Player {currentPlayer+1}'s turn</h3><p>Must start with: {lastLetter || 'any'}</p><input value={word} onChange={e=>setWord(e.target.value)} placeholder='Enter word'/><button type='submit'>Submit</button></form>); }